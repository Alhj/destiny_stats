import React, { useState } from 'react';

const StartSearch:(props:any) => JSX.Element = (props:any) => {
  const[format, setFormat] = useState('pc');
  
  return(
  <div>

  </div>
  );
};


export default StartSearch;